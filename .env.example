PORT=5000
GEMINI_API_KEY=your_gemini_api_key_here
# paste the content of serviceAccountKey.json flattened into a string if deploying to cloud without file access
# FIREBASE_SERVICE_ACCOUNT={"type": "service_account", ...}
